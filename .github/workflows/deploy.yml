name: Deploy On Server 
on:  
  workflow_dispatch:
  workflow_run:
    #1 this is previous workflow name
    workflows: ["Test Build image Docker"]
    types:
      -  completed
jobs:
  deploy:
    name: Deploy to server for simulation
    runs-on: ubuntu-latest
    #2 this is the key , using from workflow_run value to execute
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Deploy application
        run: |
            echo "Deploying application to server..."
        
    
